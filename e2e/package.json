{
  "name": "setu-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "End-to-end (Playwright) suite for SETU — backend + pilgrim + ops, offline/no-keys.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
